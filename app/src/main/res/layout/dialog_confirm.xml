<?xml version="1.0" encoding="utf-8"?>
<!--
  Reusable confirmation dialog.
  Used for: Logout, Reject applicant, Shortlist applicant.
  Window background is transparent; the card provides the visual shape.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_dialog"
    android:padding="24dp">

    <!-- Icon row (optional — host sets visibility) -->
    <LinearLayout
        android:id="@+id/layout_dialog_icon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal"
        android:gravity="center"
        android:background="@drawable/bg_icon_surface"
        android:layout_marginBottom="16dp"
        android:visibility="gone">

        <android.widget.ImageView
            android:id="@+id/iv_dialog_icon"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:src="@drawable/ic_logout"
            android:contentDescription="Dialog icon" />
    </LinearLayout>

    <!-- Title -->
    <TextView
        android:id="@+id/tv_dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirm"
        android:textColor="@color/text_primary"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:letterSpacing="0.01" />

    <!-- Message -->
    <TextView
        android:id="@+id/tv_dialog_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="24dp"
        android:text="Are you sure you want to proceed?"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.45" />

    <!-- Action buttons — stacked vertically for readability -->
    <TextView
        android:id="@+id/btn_dialog_confirm"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center"
        android:background="@drawable/bg_dialog_button_primary"
        android:text="Confirm"
        android:textColor="@color/white"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium"
        android:clickable="true"
        android:focusable="true" />

    <TextView
        android:id="@+id/btn_dialog_cancel"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:background="@drawable/bg_dialog_button_ghost"
        android:text="@string/cancel_action"
        android:textColor="@color/text_secondary"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>