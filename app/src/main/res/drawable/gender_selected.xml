<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Light blue color -->
    <corners android:radius="50dp" />  <!-- Rounded corners -->
    <solid android:color="#3900BCD4"/>
    <padding android:left="12sp" android:right="8sp" android:top="8sp" android:bottom="8sp"/>
    <stroke
        android:width="1dp"
        android:color="#00BCD4"/>
    <size android:width="60dp" android:height="20sp" />
</shape>
