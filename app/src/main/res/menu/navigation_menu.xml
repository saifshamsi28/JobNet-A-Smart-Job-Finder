<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:title="Home"
            android:icon="@drawable/home_icon"
            android:id="@+id/home" />
        <item android:title="Jobs"
            android:icon="@drawable/jobs_icon"
            android:id="@+id/job_list" />
        <item android:title="New Openings"
            android:icon="@drawable/new_openings"
            android:id="@+id/new_openings"/>
        <item android:title="History"
            android:id="@+id/view_history"
            android:icon="@drawable/history_img"/>
    </group>

    <item android:id="@+id/profile_section"
        android:title="Profile">
       <menu>
           <group android:checkableBehavior="single"
               >
               <item android:title="Login"
                   android:icon="@drawable/login_icon"
                   android:id="@+id/login" />
               <item android:title="Profile"
                   android:icon="@drawable/profile_icon"
                   android:id="@+id/profile" />
               <item android:title="Settings"
                   android:icon="@drawable/settings_icon"
                   android:id="@+id/settings" />
               <item android:title="Logout"
                   android:icon="@drawable/logout_icon"
                   android:id="@+id/logout" />
           </group>
       </menu>
    </item>

    <item android:id="@+id/share_section"
        android:title="Share">
        <menu>
            <group android:checkableBehavior="single"
                >
                <item android:title="Share"
                    android:icon="@drawable/share_icon"
                    android:id="@+id/share" />
                <item android:title="Rate Us"
                    android:icon="@drawable/rate_icon"
                    android:id="@+id/rate" />
            </group>
        </menu>
    </item>
</menu>